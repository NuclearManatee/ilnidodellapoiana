---
import Header from './Header.jsx';
import { NAV_MENU } from '../../config';
import './header.css';

export interface Props {
    pathname : string
}

const {pathname} = Astro.props;
---

<Header options={NAV_MENU} pathname={pathname} />